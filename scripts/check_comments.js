const fs=require('fs'); const path=require('path'); const p=path.resolve(__dirname,'..','popup','popup_main.js'); const s=fs.readFileSync(p,'utf8'); let i=0; let state=null; let problems=[]; while(i<s.length){ const ch=s[i]; const next=s[i+1]; if(!state){ if(ch==='/'&&next==='*'){ state='/*'; i+=2; continue;} if(ch==='/'&&next==='/'){ // single line comment
 i+=2; while(i<s.length && s[i]!=="\n") i++; continue;} i++; } else if(state==='/*'){ if(ch==='*'&&next==='/'){ state=null; i+=2; continue;} i++; } } if(state==='/*') { console.error('Unterminated block comment (/* not closed)'); process.exit(1);} else { console.log('No unterminated block comments found'); }