const fs = require('fs'); const path = require('path'); const p = path.resolve(__dirname, '..', 'content', 'content_main.js'); const s = fs.readFileSync(p,'utf8'); const i = Number(process.argv[2]); if (!i) { console.log('usage: node show_index.js <index>'); process.exit(1);} const upTo = s.slice(0,i); const lines = upTo.split(/\r?\n/); const lineNum = lines.length; const col = lines[lines.length-1].length + 1; const all = s.split(/\r?\n/); const start = Math.max(0, lineNum-3); const end = Math.min(all.length, lineNum+2); console.log('index', i, '-> line', lineNum, 'col', col); for (let ln=start; ln<end; ln++) { const prefix = (ln+1===lineNum)?'>> ':'   '; console.log(prefix + (ln+1) + ': ' + all[ln]); }