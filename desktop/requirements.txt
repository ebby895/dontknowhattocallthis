# Core
httpx>=0.27           # redirect chain resolution, Gemini, listing verification
playwright>=1.45      # drives a real logged-in Chrome profile for x.com

# UI
PyQt6>=6.7            # main window, widgets
PyQt6-Qt6>=6.7

# Secrets - keys go to the OS credential store, never to a file
keyring>=25.0

# Optional: OS-level keyboard/mouse input mode
pyautogui>=0.9.54
