# STATE

Rolling session state file. Update in place; keep it short.

## Session rules
- Token-saving plan: single pass. Think before spending tokens.
- Batch reads/searches; avoid re-reading files already summarized here.
- Record findings and decisions here so future sessions don't re-derive them.

## Repo
- `ebby895/dontknowhattocallthis` — Chrome extension (manifest.json, background.js,
  popup/, content/, offscreen/, lib/, shared/, plugins/, scripts/, auth/, src/).
- Supabase backend (SUPABASE_README.md). Other context: README_DEV.md,
  project_context.md, audit_report.md, scheduler_reference.md.
- Dev branch for this work: `claude/token-usage-optimization-i5syyv`.

## Current status
- 2026-08-23: STATE.md created. No task assigned yet — awaiting the actual request.

## Open items
- (none)
